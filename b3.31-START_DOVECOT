#! /bin/bash

sudo cp -vf xlapp-bootscripts/dovecot /etc/init.d/
sudo chmod 0754 /etc/init.d/dovecot

sudo xlapp-bootscripts/boot-links-create -s 24 -k 49 dovecot

#
# At this point, dovecot still needs configuration...
#
