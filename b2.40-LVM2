#! /bin/bash

./configure --prefix=/usr --enable-pkgconfig --enable-dmeventd --enable-cmdlib

make

sudo make install

LVM2_DOC_DIR=/usr/share/doc/lvm2-2.02.90
sudo install -d ${LVM2_DOC_DIR}
sudo cp -va doc/* ${LVM2_DOC_DIR}
sudo rm -vf ${LVM2_DOC_DIR}/Makefile{,.in}
